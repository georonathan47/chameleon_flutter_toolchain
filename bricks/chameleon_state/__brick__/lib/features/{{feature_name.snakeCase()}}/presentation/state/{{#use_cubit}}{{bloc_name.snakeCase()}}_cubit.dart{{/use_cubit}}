import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:injectable/injectable.dart';

import '{{bloc_name.snakeCase()}}_state.dart';

// Scaffolded, not wired to a repository — chameleon_state doesn't know one.
// Replace the body of start() with the real work this cubit should do;
// the Initial/InProgress/Success/Failure shape is the part worth keeping
// (guardrail #14: sealed states, fully exhausted by callers).
@injectable
class {{bloc_name.pascalCase()}}Cubit extends Cubit<{{bloc_name.pascalCase()}}State> {
  {{bloc_name.pascalCase()}}Cubit() : super(const {{bloc_name.pascalCase()}}Initial());

  Future<void> start() async {
    emit(const {{bloc_name.pascalCase()}}InProgress());
    emit(const {{bloc_name.pascalCase()}}Success());
  }
}
