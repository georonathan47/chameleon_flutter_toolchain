import 'package:flutter_test/flutter_test.dart';

import 'package:{{project_name.snakeCase()}}/features/{{feature_name.snakeCase()}}/presentation/state/{{bloc_name.snakeCase()}}_controller.dart';
import 'package:{{project_name.snakeCase()}}/features/{{feature_name.snakeCase()}}/presentation/state/{{bloc_name.snakeCase()}}_state.dart';

void main() {
  test('starts in the Initial state', () {
    expect({{bloc_name.pascalCase()}}Controller().state, const {{bloc_name.pascalCase()}}Initial());
  });

  test('start() transitions [InProgress, Success]', () async {
    final controller = {{bloc_name.pascalCase()}}Controller();
    final seen = <{{bloc_name.pascalCase()}}State>[];
    controller.addListener(() => seen.add(controller.state));

    await controller.start();

    expect(seen, const [
      {{bloc_name.pascalCase()}}InProgress(),
      {{bloc_name.pascalCase()}}Success(),
    ]);
  });
}
